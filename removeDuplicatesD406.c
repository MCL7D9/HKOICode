#include <stdio.h>
#include <stdbool.h>

int main()
{
        int ls[20] = {0};
        unsigned char num=0;
        scanf("%hhu%", &num);
        for (unsigned char i = 0; i < num; i++){
                bool flag = false;
                for (unsigned char j = 0; j < )
        }
}
